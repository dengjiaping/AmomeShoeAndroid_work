<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f5f5f5" >

    <include
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        layout="@layout/head_title" />

    <TextView
        android:id="@+id/tv_tip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/apptwocode_btn"
        android:layout_below="@+id/title"
        android:layout_marginTop="14dp"
        android:text="已买鞋的用户请扫描标牌上的二维码，解锁app"
        android:textColor="@color/login_gray" />

    <Button
        android:id="@+id/apptwocode_btn"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_below="@+id/tv_tip"
        android:layout_marginBottom="20dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginTop="50dp"
        android:background="@drawable/paypicture"
        android:gravity="center"
        android:text="扫描二维码"
        android:textColor="@android:color/white" />

    <Button
        android:id="@+id/apppay_btn"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_below="@+id/apptwocode_btn"
        android:layout_marginBottom="40dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:background="@drawable/paypicture"
        android:text="微信支付"
        android:textColor="@android:color/white" />

</RelativeLayout>