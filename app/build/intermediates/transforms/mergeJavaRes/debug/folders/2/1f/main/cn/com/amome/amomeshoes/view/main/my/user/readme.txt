1、MyInformationActivity 个人信息主页面，默认不可编辑。点击右上角编辑按钮可以进行资料修改。点击头像进入头像修改页面（ClipAvatarActivity）。
2、ClipAvatarActivity 头像修改页面。分为拍照和相册选择两种添加方式。选择图片后可以对图片进行简单裁剪，再确定。
3、HobbyListActivity 爱好页面。展示服务端返回的爱好列表。
4、JobListActivity 工作页面。展示服务端返回的职业列表。
